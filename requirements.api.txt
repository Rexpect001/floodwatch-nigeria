# API Gateway — FastAPI service
# Pin minor versions for reproducibility; update quarterly

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
uvloop==0.20.0                 # Fast event loop (used by Uvicorn --loop uvloop)
python-multipart==0.0.9        # Form parsing (USSD callback)

# Database
asyncpg==0.29.0                # Async PostgreSQL driver
psycopg2-binary==2.9.9         # Sync fallback / migrations

# Cache & pub/sub
redis[hiredis]==5.0.8          # Redis async client

# Auth & security
python-jose[cryptography]==3.3.0   # JWT validation
passlib[bcrypt]==1.7.4

# Data validation
pydantic==2.9.0
pydantic-settings==2.5.2

# HTTP client
httpx==0.27.2

# AI / Voice pipeline
anthropic>=0.40.0

# Utils
python-dotenv==1.0.1
pytz==2024.2
orjson==3.10.7                 # Fast JSON (replaces stdlib json in responses)

# Monitoring
prometheus-fastapi-instrumentator==7.0.0

# Compression (GZip middleware already in FastAPI)
# — no extra dep needed
